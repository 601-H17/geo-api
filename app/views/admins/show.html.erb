<div class="container">
  <p id="notice"><%= notice %></p>

  <p>
    <strong>Nom d'utilisateur:</strong>
    <%= @admin.username %>
  </p>

  <p>
    <strong>Adresse courriel:</strong>
    <%= @admin.email %>
  </p>

  <p>
    <strong>Maître administrateur:</strong>
    <%= @admin.master_admin %>
  </p>

  <% if current_admin.master_admin? || @admin.id == current_admin.id %>
      <%= link_to 'Modifier', edit_admin_path(@admin) %> |
  <% end %>
  <%= link_to 'Revenir', admins_path %>
</div>