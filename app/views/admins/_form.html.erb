<%= render 'shared/errors', obj: @admin %>

<%= form_for(@admin) do |f| %>
    <div class="row">
      <div class="group input-field col s12">
        <%= f.text_field :username, class: 'validate' %>
        <label>Nom d'utilisateur</label>
      </div>
    </div>
    <div class="row">
      <div class="group input-field col s12">
        <%= f.text_field :email, class: 'validate' %>
        <label>Courriel</label>

      </div>
    </div>

    <div class="row">
      <%= f.check_box :master_admin, id: 'master-admin' %>
      <label for="master-admin">Administrateur principal</label>
    </div>

    <div class="row">
      <%= f.submit ('Créer'), class: 'waves-effect waves-light btn' %>
    </div>

<% end %>
